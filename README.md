#  Advent of Code 2025

The implementation is provided as CLI application compatible macOS and Linux.

To test it out, download the repo, and run `swift run` from within it.
This requires a Swift toolchain from swift.org.

You need to provide the current working directory.
