#  Advent of Code 2025

The implementation is provided as CLI application compatible with macOS
on both Apple Silicon and Intel.

To test it out, download the repo, and run `swift run` from within it.

You need to provide the current working directory.
